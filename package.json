{
  "name": "cash-cannon",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Finger-gun hand-tracking money cannon in the browser.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
